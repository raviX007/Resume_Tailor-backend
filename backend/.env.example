# ─── Required ───────────────────────────────────────────────────
OPENAI_API_KEY=sk-...

# ─── Optional — LLM fallback (used if OpenAI fails 5 consecutive times) ──
GOOGLE_AI_API_KEY=

# ─── Optional — Langfuse prompt management + tracing ────────────
# Without these keys the app still works — embedded fallback prompts are used.
# Set these to enable versioned prompt management and LLM tracing in Langfuse.
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_HOST=https://cloud.langfuse.com

# ─── App config ─────────────────────────────────────────────────
LLM_MODEL=
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
LOG_LEVEL=INFO

# ─── UI auth gate ────────────────────────────────────
# Set both to enable. Leave empty to disable auth (dev mode).
AUTH_USERNAME=
AUTH_PASSWORD=
